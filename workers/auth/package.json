{
  "name": "brimba-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Brimba's auth worker: email-code login, Google login, sessions. All user data in our own D1.",
  "scripts": {
    "dev": "wrangler dev --env staging --port 8787 --local",
    "test": "vitest run",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
