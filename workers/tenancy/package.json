{
  "name": "brimba-tenancy",
  "version": "0.1.0",
  "private": true,
  "description": "Brimba's tenancy worker: teams, memberships, and the per-team database factory.",
  "scripts": {
    "dev": "wrangler dev --env staging --port 8788 --inspector-port 9231 --local --persist-to ../../.dev-state",
    "test": "vitest run",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
