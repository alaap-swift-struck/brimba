{
  "name": "brimba-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Brimba's front door: serves the screens, routes /api to the domain workers.",
  "scripts": {
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.3"
  }
}
